# nimbus v1 テキストスコープ決定記録

v1 で提供するテキスト関連機能の**原案 vs 決定**を残す記録（なぜその v1 スコープにしたかの根拠）。
v1 でやる決定の多くは既に実装済み（TextField / TextArea 等）。**「初版ではやらない／いずれ必要」項目は
[doc/internal/text_backlog.md](doc/internal/text_backlog.md) で実装追跡する**（このファイルは根拠の記録、追跡は backlog 側）。
長期的に確定した設計方針は `CLAUDE.md` / 各 `*/doc` に置く。バックログ運用規約は [doc/internal/backlog.md](doc/internal/backlog.md)。

## v1 テキスト機能スコープ

[2026-05-17 整理]

GUI フレームワークとして v1 で提供するテキスト関連機能の方針。
原案 (◯ = 初版推奨 / △ = どちらも有り / ✗ = 非推奨) と最終決定をペアで残す。

### Label / Button / 表示系

**1. 単一行テキスト描画**
- 原案: ◯ 大前提
- 決定: 初版でやる

**2. 折り返し: あふれた位置で次行 (文字単位)**
- 原案: ◯ 簡素実装、CJK 用途では実用
- 決定: 初版ではやらない（Label/Button が一行のみ許容のため消費先がない）

**3. 折り返し: 空白で break (Latin 単語境界)**
- 原案: ◯ "Hello\nWorld" 風に切れる、英語 UI で必須
- 決定: 初版では Label/Button も一行のみ許容、それ以降でもやるか不明

**4. 折り返し: CJK 禁則処理**
- 原案: △ テーブル数十エントリで実装可、入れるか v2 か
- 決定: 初版では Label/Button も一行のみ許容、それ以降でもやるか不明

**5. テキスト省略 (`Long fil...`)**
- 原案: △ Button や ListItem で頻出。比較的安価
- 決定: 初版ではやらない。が、確実にいずれ必要

**6. Tab stops (`\t` で揃える)**
- 原案: ✗ 等幅相当の挙動だけ用意して、本物は v2
- 決定: 初版ではやらない。が、確実にいずれ必要

**7. リッチテキスト (1 Label 内で色/太字/サイズ混在)**
- 原案: ✗ 別 widget が必要、複雑、v2+
- 決定: 初版ではやらない。が、確実にいずれ必要

**8. インライン画像 (`<img>` 相当を Label に)**
- 原案: ✗ リッチテキストの一部、v2+
- 決定: 初版ではやらない。それ以降でもやるか不明

**9. 右→左 (BiDi)**
- 原案: ✗ 文字コードセクションで非対応宣言済み
- 決定: やらない

### TextField (単一行入力)

**10. 文字挿入 / Backspace (codepoint 単位)**
- 原案: ◯ 最低限
- 決定: 初版でやる。のちのちクラスター単位に移行できるように考えておく

**11. Backspace / Delete (grapheme cluster 単位)**
- 原案: △ TextField で書記素単位は将来要望済み。v1 で入れるか?
- 決定: 初版ではやらない

**12. 矢印キー (1 文字単位)**
- 原案: ◯ 最低限
- 決定: 初版ではクラスター単位で移動できなくても OK だが、確実にいずれ必要

**13. Home / End**
- 原案: ◯ 行頭・行末
- 決定: 初版ではやらない。優先度も高くないがやる

**14. Ctrl+矢印 (word jump)**
- 原案: △ Word break 必要。空白判定だけの簡易実装で v1 でも可
- 決定: 初版ではやらない。が、確実にいずれ必要

**15. クリックでカーソル位置決め**
- 原案: ◯ 必須
- 決定: 初版でやる

**16. ドラッグで選択**
- 原案: ◯ 必須
- 決定: 初版でやる

**17. Shift+矢印 で選択拡張**
- 原案: ◯ 必須
- 決定: 初版でやる

**18. ダブルクリックで単語選択**
- 原案: △ Word break 依存。v1 でやらない手もある
- 決定: 初版ではやらない。が、確実にいずれ必要

**19. Ctrl+A で全選択**
- 原案: ◯ 必須
- 決定: 初版でやる

**20. Cut / Copy / Paste (Ctrl+X/C/V)**
- 原案: ◯ システムクリップボード経由、GLFW が UTF-8 で提供
- 決定: 初版でやる

**21. Undo / Redo**
- 原案: △ 実装重い。v1 で入れるか v2 か
- 決定: 初版ではやらない。確実に必要かも不明。というのも利用者側で実装可能でもある。とはいえ一般的なユースケースではあるので、提供したほうがいい

**22. Password モード (`*****`)**
- 原案: △ グリフ差し替えだけ、安価
- 決定: いらない

**23. Placeholder text**
- 原案: △ 安価、UX 的に欲しい
- 決定: 初版ではやらない。が、確実にいずれ必要

**24. Max length 制約**
- 原案: ◯ 安価
- 決定: 初版ではやらない。優先度も低いがいずれやる

**25. Read-only モード**
- 原案: ◯ 安価、widget の flag 1 つ
- 決定: 初版でやる

### TextArea (複数行入力)

**26. 複数行表示 + ↑↓ で行移動**
- 原案: △ TextField より段違いに重い。v1 で入れるか?
- 決定: TextArea 自体は初版でやる。クラスター単位の移動などは完全に入っている必要はない（TextField 同様）

**27. Page Up/Down**
- 原案: △ TextArea が入れば付随
- 決定: 初版でやる

**28. Ctrl+Home/End (文書頭/末)**
- 原案: △ 同上
- 決定: 初版でやる

**29. Word wrap (TextArea 内で自動折返し)**
- 原案: △ Line break iterator 必要
- 決定: 初版ではやらない

**30. 横スクロール (折返し無し時)**
- 原案: △ スクロールバー widget 依存
- 決定: 初版でやる。また、スクロールバーは Swing 同様汎用的に設計される（テキストボックス専用ではない）

### IME / 高度

**31. IME 確定文字の受信**
- 原案: ◯ GLFW char callback、無料
- 決定: 初版ではやらないつもりでしたが、推奨とのことなのでやる

**32. IME 変換中の inline 表示 (未確定下線等)**
- 原案: ✗ プラットフォーム API 直叩き必要、重い、v2+
- 決定: 初版ではやらない

**33. スペルチェック**
- 原案: ✗ OS 連携 or 辞書、v2+
- 決定: 初版ではやらない。以後もおそらくやらない

**34. ネイティブコンテキストメニュー (右クリック)**
- 原案: △ nimbus 内で構築 (Cut/Copy/Paste) なら可、OS native は ✗
- 決定: 自前でコンテキストメニューを描画する（メニューバーも自前描画の可能性あり）。また、一応ネイティブに切り替えられるように考えたい

**35. スクリーンリーダ (accessibility tree)**
- 原案: ✗ 完全に別レイヤ、v2+
- 決定: 初版ではやらない。以後もやるか不明

### 描画演出

**36. Bold / Italic**
- 原案: △ 別ファイルロード or freetype 合成。最初は regular のみで割切可
- 決定: 初版ではやらない。が、確実にいずれ必要

**37. Underline / Strikethrough**
- 原案: △ 線描画追加、安価
- 決定: 初版ではやらない。が、確実にいずれ必要

**38. Text shadow / outline**
- 原案: ✗ v2+
- 決定: 初版ではやらない。優先度も低い

**39. キャレット点滅**
- 原案: ◯ TextField に必須
- 決定: 初版でやる

### システム連携

**40. システムクリップボード (UTF-8)**
- 原案: ◯ GLFW 経由、ほぼ無料
- 決定: 初版でやる

**41. テキスト drag&drop**
- 原案: ✗ v2+
- 決定: 初版ではやらない。テキストに限らずドラッグアンドドロップは考えたい

**42. I-beam カーソル (hover で形変化)**
- 原案: ◯ GLFW の SetCursor、安価
- 決定: 初版ではやらない。ウィンドウリサイズ時やスプリットペインにおけるカーソル変化など、（カーソル変化機構自体は）必須ではある

### 設計上の補足

v1 でやらないが、API 設計時に将来を見越して抽象を整えるもの:

- **codepoint → grapheme cluster 移行** (#10/#12): TextField の Backspace / カーソル移動 API は v1 では codepoint 単位だが、将来 grapheme cluster 単位に差し替え可能な抽象 (`countCharacters` / `deleteBackward` 等) で設計する。バイト index を直接公開しない。
- **メニュー類は自前描画** (#34): コンテキストメニュー / メニューバー含めて nimbus 内で描画する方針。ただし将来 native メニュー（Windows: `TrackPopupMenu` / Mac: `NSMenu`）に切り替えられる抽象を残す。
    - 抽象を残す、はナシにしたい。メニューに自前コンポーネントを載せられることを優先。
- **ドラッグ&ドロップは汎用機能** (#41): テキスト D&D 専用ではなく、widget 全般の D&D を扱う仕組みとして設計する（v1 では未実装）。
- **スクロールバーは汎用 widget** (#30): Swing の `JScrollBar` / `JScrollPane` 同様、テキスト専用ではない汎用部品として設計する。TextArea の横スクロールはこれを使う。
- **汎用 SetCursor 機構** (#42): I-beam は v1 で配線しないが、ウィンドウリサイズ / スプリットペイン用にカーソル変化機構自体は v1 で必要。

# writing_style_hint

編集中。AI生成の文章が読みにくいことを言語化してみたい。
実際の例を見ながら考える。

## アスタリスクの多用を避ける

これがオリジナルの文章です。
```md
## 入力イベントとタスクを同じキューに並べる理由
1. **単一の入口**: GLFW から来る生の入力、`invokeLater` で投入されるタスク、テスト用の合成入力イベントがすべて同じ FIFO を通る。デバッグ時に「どの順番で何が起きるか」を 1 か所だけ見ればよい
2. **post API の公開**: `postEvent` が公開 API なので、テスト / マクロ / IME の文字確定通知 / accessibility tool 等が外部から入力イベントを差し込める（Java AWT の `EventQueue.postEvent` と同じ）
3. **ordering の保証**: 「ボタン押下 → ハンドラ内で `invokeLater(redraw)` → 次のクリック」のような並びが post された順に厳密に再生される
4. **mouse-move coalescing の足場**: 入力イベントもキューにあるので、将来「連続する `.move` を 1 個にまとめる」最適化を入れやすい（機能要望）

トレードオフは「OS コールバックから widget に届くまで 1 イベントループ分の latency が乗る」だが、60fps なら 16ms 未満で体感はほぼ無い。
```

アスタリスクを消したバージョン。これでもよくない？という提案。
が、アスタリスクが有用な場合もあるので、分かりやすいルールにはできません。
```md
## 入力イベントとタスクを同じキューに並べる理由
1. 単一の入口: GLFW から来る生の入力、`invokeLater` で投入されるタスク、テスト用の合成入力イベントがすべて同じ FIFO を通る。デバッグ時に「どの順番で何が起きるか」を 1 か所だけ見ればよい
2. post API の公開: `postEvent` が公開 API なので、テスト / マクロ / IME の文字確定通知 / accessibility tool 等が外部から入力イベントを差し込める（Java AWT の `EventQueue.postEvent` と同じ）
3. ordering の保証: 「ボタン押下 → ハンドラ内で `invokeLater(redraw)` → 次のクリック」のような並びが post された順に厳密に再生される
4. mouse-move coalescing の足場: 入力イベントもキューにあるので、将来「連続する `.move` を 1 個にまとめる」最適化を入れやすい（機能要望）

トレードオフは「OS コールバックから widget に届くまで 1 イベントループ分の latency が乗る」だが、60fps なら 16ms 未満で体感はほぼ無い。
```


## 日本語に寄せる

上記の改善版にもまだ不満があり、固有名詞じゃない、対応する一般的な日本語表記がある場合でもアルファベットになっているケースです。
ただしメソッド名など、コード中のシンボルは日本語にしない方がいい。
以下、私が書きなおしたバージョン。
```md
## 入力イベントとタスクを同じキューに並べる理由
1. 単一の入口: GLFW から来る生の入力、`invokeLater` で投入されるタスク、テスト用の合成入力イベントがすべて同じ FIFO を通る。デバッグ時に「どの順番で何が起きるか」を 1 か所だけ見ればよい
2. post API の公開: `postEvent` が公開 API なので、テスト / マクロ / IME の文字確定通知 / アクセシビリティツール 等が外部から入力イベントを差し込める（Java AWT の `EventQueue.postEvent` と同じ）
3. 順序の保証: 「ボタン押下 → ハンドラ内で `invokeLater(redraw)` → 次のクリック」のような並びが post された順に厳密に再生される
4. マウスイベント合体の足場: 入力イベントもキューにあるので、将来「連続する `.move` を 1 個にまとめる」最適化を入れやすい（機能要望）

トレードオフは「OS コールバックからウィジェットに届くまで 1 イベントループ分のレイテンシが乗る」だが、60fps なら 16ms 未満で体感はほぼ無い。
```

## 可能なら折り返さないようにする

さらにまだ改善できます。せっかく箇条書きを採用しているのに行＝アイテムになっていません。ひとつのアイテムが行をまたぐ場合があるのです。
これは私のモニターやフォントサイズ都合なので、Claudeが気を付けてどうこうなるものではありません。（linterなどで静的検知？）
以下、私が書きなおしたバージョン。
```md
## 入力イベントとタスクを同じキューに並べる理由
* 単一の入口
  * GLFW から来る生の入力、`invokeLater` で投入されるタスク、テスト用の合成入力イベントがすべて同じ FIFO を通る。
  * デバッグ時に「どの順番で何が起きるか」を 1 か所だけ見ればよい
* post API の公開
  * `postEvent` が公開 API なので、テスト / マクロ / IME の文字確定通知 / アクセシビリティツール 等が外部から入力イベントを差し込める
  * （Java AWT の `EventQueue.postEvent` と同じ）
* 順序の保証
  *  「ボタン押下 → ハンドラ内で `invokeLater(redraw)` → 次のクリック」のような並びが post された順に厳密に再生される
* マウスイベント合体の足場: 入力イベントもキューにあるので、将来「連続する `.move` を 1 個にまとめる」最適化を入れやすい（機能要望）

トレードオフは「OS コールバックからウィジェットに届くまで 1 イベントループ分のレイテンシが乗る」だが、60fps なら 16ms 未満で体感はほぼ無い。
```

なんとなく順序なしリストにしたけど、特にこだわりはない。処理順序や依存関係が絡む場合は順序付きリストの方がいいのかも？

## 造語（あるいは作者と語彙の共有ができていない）

"point-of-need" という語が使われたことが何度かあり、私は文脈から「必要になるまで作らない」と解釈した。
で、これは別の適当な日本語はないのか？
一方で、語彙が共有できていれば単純に文章の圧縮につながるので、悪い面ばかりではない。

## アスキーアート

たとえばこんなの。
```
tools/apigen/nimbus.api          （入力スペック / ABI 表面の真実）
tools/apigen/preamble.h          （手書き: C ヘッダ前置き）
tools/apigen/preamble_protos.h   （手書き: C プロト。typedef の後に差し込み）
tools/apigen/preamble.zig        （手書き: Zig ランタイム支援前置き）
tools/apigen/preamble_ir.txt     （手書き: IR エントリ。生成 IR にマージ）
        │
        ▼  zig build apigen   （tools/apigen/main.zig を host で実行）
        │
        ├─► include/nimbus.h          = preamble.h + 生成 typedef + preamble_protos.h + 生成プロト + 末尾
        ├─► framework/src/c_api.zig   = preamble.zig + 生成 export fn シム
        └─► bindings/nimbus_api.json  = 生成 IR に preamble_ir.txt の手書きエントリをマージ
```

あなたはAIなのでこういった図をわりとぴったり整列させながら生成できる。
いっぽうで、ドキュメントを編集するのはAIだけではない。
人間が編集しようと思ったときにずれないように調整するのは面倒。
が、アスキーアートが分かりにくい、とは思わない。どうしたもんか。